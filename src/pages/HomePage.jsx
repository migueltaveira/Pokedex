/*import PokemonCard from "../components/PokemonCard";
import SearchBar from "../components/SearchBar";
import { featuredPokemons } from "../services/pokemonService";

function HomePage() {
  return (
    <section className="content-grid">
      <div className="content-grid__intro">
        <p className="section-label">Página inicial</p>
        <h2>Explore sua Pokédex</h2>
        <p>
          A base do projeto já está separada por responsabilidade para facilitar
          crescimento, manutenção e novas features.
        </p>
        <SearchBar />
      </div>

      <div className="pokemon-grid">
        {featuredPokemons.map((pokemon) => (
          <PokemonCard key={pokemon.id} pokemon={pokemon} />
        ))}
      </div>
    </section>
  );
}

export default HomePage;*/
